<UserControl
    x:Class="netflix.Main.Views.BookMarkedView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:netflix.Main.Views"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <Grid>
        <ListBox
            x:Name="MyListListBox"
            Margin="8"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            ItemsSource="{Binding MyList}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="128" Margin="8">
                        <Image Height="128" Source="https://img.icons8.com/plasticine/128/000000/film-reel.png" />
                        <TextBlock
                            Margin="0,8,0,0"
                            FontWeight="Bold"
                            Text="{Binding Title}"
                            TextAlignment="Center" />
                        <TextBlock
                            FontSize="10"
                            Text="{Binding Description}"
                            TextAlignment="Center" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>